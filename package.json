{
  "name": "m12-models-dev",
  "private": true,
  "scripts": {
    "prepare": "node setup-hooks.js"
  },
  "devDependencies": {
    "@commitlint/cli": "^17.5.0",
    "@commitlint/config-conventional": "^17.5.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.0.0"
  },
  "lint-staged": {
    "*.php": [
      "vendor/bin/pint --config=pint.json --dirty"
    ]
  }
}
