{
  "name": "m12-models",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "dev:hooks": "node ./setup-hooks.js"
  },
  "devDependencies": {
    "husky": "^9.0.0",
    "lint-staged": "^14.0.0",
    "@commitlint/cli": "^17.0.0",
    "@commitlint/config-conventional": "^17.0.0"
  },
  "lint-staged": {
    "*.php": [
      "vendor/bin/pint",
      "git add"
    ]
  }
}
